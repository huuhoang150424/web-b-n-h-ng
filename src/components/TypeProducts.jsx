



const TypeProducts = ({name}) => {
    return (
        <div className="px-[12px] py-[8px] cursor-pointer hover:bg-gray-300 transition-all delay-100 ">
            <p className="text-[1.4rem] font-[500] text-textColor ">{name}</p>
        </div>
    );
};

export default TypeProducts;